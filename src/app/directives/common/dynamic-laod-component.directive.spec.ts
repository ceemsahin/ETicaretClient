import { DynamicLaodComponentDirective } from './dynamic-laod-component.directive';

describe('DynamicLaodComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicLaodComponentDirective();
    expect(directive).toBeTruthy();
  });
});
